<?xml version="1.0"?>
<ruleset name="WordPress Features API Tests">
	<description>WordPress Features API PHP_CodeSniffer ruleset for test files.</description>

	<!-- Include the WordPress standard -->
	<rule ref="WordPress">
		<!-- Add any rules you want to exclude here -->
		<exclude name="WordPress.Arrays.MultipleStatementAlignment.DoubleArrowNotAligned" />
		<exclude name="Generic.Formatting.MultipleStatementAlignment.NotSameWarning" />
	</rule>

	<!-- Include WordPress-Extra for additional best practices -->
	<rule ref="WordPress-Extra" />

	<!-- Let's also check that everything is properly documented -->
	<rule ref="WordPress-Docs" />

	<!-- Add in some extra rules from other standards -->
	<rule ref="Generic.CodeAnalysis.UnusedFunctionParameter" />
	<rule ref="Generic.Commenting.Todo" />

	<!-- Check for PHP cross-version compatibility - tests can use PHP 7.4 features -->
	<config name="testVersion" value="7.4-" />
	<rule ref="PHPCompatibilityWP" />

	<!-- Set tab width -->
	<arg name="tab-width" value="4" />

	<!-- Show progress, show sniff codes in all reports, and use colors -->
	<arg value="ps" />
	<arg name="colors" />

	<!-- Scan only test files -->
	<file>./tests</file>

	<!-- Specifying extensions to check -->
	<arg name="extensions" value="php" />

	<!-- WordPress specific configuration -->
	<config name="minimum_supported_wp_version" value="5.9" />

	<!-- Rules for WordPress Functions -->
	<rule ref="WordPress.WP.DeprecatedFunctions">
		<properties>
			<property name="minimum_wp_version" value="5.9" />
		</properties>
	</rule>
</ruleset>
