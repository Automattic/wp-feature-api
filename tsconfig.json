{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  // Prevent root-level compilation
  "files": [],
  // Define references to the sub-projects
  "references": [
    { "path": "./packages/client" },
    { "path": "./packages/client-features" },
    { "path": "./demo/wp-feature-api-demo" }
    // Add other packages/apps here if they use TypeScript
  ]
}
