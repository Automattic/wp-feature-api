{
	"name": "mcp-ping-server",
	"version": "1.0.0",
	"description": "Simple MCP server that responds with pong to ping",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"start": "node dist/index.js"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.8.0"
	},
	"devDependencies": {
		"typescript": "^5.0.0",
		"@types/node": "^18.0.0"
	}
}
